请在有本地终端权限且由用户授权的环境处理这个项目。
先读README，运行测试和 `python deploy.py --dry-run`，然后运行 `python deploy.py`。
用户已要求创建公开a-share-brief网页，可通过官方gh工具创建这个项目并开启Pages。
缺少gh或Python则指导使用官方渠道安装。需要浏览器授权时交由用户本人完成，不能收集密码、验证码、token，也不能读取浏览器Cookies。
只发布deploy.py白名单文件；不要碰既有个人主页或其他仓库，不覆盖同名仓库，不强推。
若部署成功，验证实际HTTP响应和项目标记，再交付真实网址。没有成功运行就不能声称上线。
不要将空界面说成实时股票服务。真实数据、研究生产、推送均未接通；不能用假行情、旧新闻或持续更新的时间戳营造正常运行。
仅有只读GitHub连接、不具备本地授权终端时，明确说明不能执行，不反复要求用户进行无法增加写权限的连接操作。
